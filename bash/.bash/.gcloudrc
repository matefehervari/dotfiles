
# The next line updates PATH for the Google Cloud SDK.
if [ -f '/$HOME/util/google-cloud-sdk/path.bash.inc' ]; then . '/home/mate/util/google-cloud-sdk/path.bash.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/$HOME/util/google-cloud-sdk/completion.bash.inc' ]; then . '/home/mate/util/google-cloud-sdk/completion.bash.inc'; fi
